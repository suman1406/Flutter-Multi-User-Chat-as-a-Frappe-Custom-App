from setuptools import setup, find_packages

setup(
    name='frappe_chat',
    version='1.0.0',
    description='Multi-user chat system',
    author='TuriumAI',
    packages=find_packages(),
    include_package_data=True,
    zip_safe=False,
)
